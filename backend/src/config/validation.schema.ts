// Validation schema is optional - remove this file or install joi package
// For now, we'll export an empty object to prevent import errors
export const validationSchema = {
  // Schema validation can be added later with joi package
};

